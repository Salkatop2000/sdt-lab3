{
  "name": "sdt-lab3",
  "displayName": "SDT_lab3",
  "description": "SDT_lab3",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.106.1"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "sdt-lab3.if-gener",
        "title": "Generate if"
      },
      {
        "command": "sdt-lab3.else-gener",
        "title": "Generate else"
      },
      {
        "command": "sdt-lab3.if-else-gener",
        "title": "Generate if-else"
      },
      {
        "command": "sdt-lab3.for-gener",
        "title": "Generate for"
      },
      {
        "command": "sdt-lab3.while-gener",
        "title": "Generate while"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.106.1",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "typescript-eslint": "^8.46.3",
    "eslint": "^9.39.1",
    "typescript": "^5.9.3",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2"
  }
}
